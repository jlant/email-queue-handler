# Email Queue Handler — application configuration.
#
# Resolution order for every setting: environment variable > this file > built-in
# default. Secrets (passwords) are deliberately NOT placed here; supply them via
# environment variables so they never live in version control:
#   EQH_SQLSERVER_PASSWORD, EQH_EMAIL_PASSWORD
#
# Environment-variable names follow the pattern EQH_<SECTION>_<KEY>, e.g.
# the [sqlserver] host below is overridden by EQH_SQLSERVER_HOST.

[app]
name = "email-queue-handler"
env = "DEV"                 # DEV | TEST | PROD. PROD requires all connection fields.
log_level = "INFO"
log_file = "logs/email_queue_handler.log"

[sqlserver]
driver = "ODBC Driver 18 for SQL Server"
host = ""                   # e.g. "SQL01" or "SQL01\\INSTANCE,1433"
database = "OracleInterface"
user = ""                   # SQL login username; password via EQH_SQLSERVER_PASSWORD
encrypt = "yes"             # Driver 18 default; leave "yes".
trust_cert = "no"           # "yes" only for an internal/self-signed cert you trust.

[email]
smtp_server = ""            # internal SMTP relay hostname
smtp_port = 587             # 587 = STARTTLS
send_from = ""              # the authenticated sender; password via EQH_EMAIL_PASSWORD
send_to = ""                # administrator address for critical failure alerts
